const Tool = () => {
  return (
    <div>
      This is my tool component! It shows some state that lives inside my host:
      (add something from a useSelector here later.).
      <button>Click me to interact with the shared API module.</button>
    </div>
  );
};

export default Tool;
